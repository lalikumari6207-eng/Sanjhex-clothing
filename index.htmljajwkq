<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>SANJHEX — Unisex Streetwear</title>
<link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:ital,wght@0,400;0,600;0,700;0,800;0,900;1,700&family=Inter:wght@300;400;500;600&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #080808;
  --surface: #101010;
  --surface2: #181818;
  --surface3: #222;
  --border: #282828;
  --accent: #E8FF47;
  --accent-dim: rgba(232,255,71,0.12);
  --red: #FF3B3B;
  --text: #F2F2F2;
  --muted: #666;
  --muted2: #999;
  --white: #fff;
}

*{margin:0;padding:0;box-sizing:border-box;}
html{scroll-behavior:smooth;}
body{
  background:var(--bg);
  color:var(--text);
  font-family:'Inter',sans-serif;
  overflow-x:hidden;
  cursor:none;
}

/* ── CURSOR ── */
#cur{width:8px;height:8px;background:var(--accent);border-radius:50%;position:fixed;pointer-events:none;z-index:99999;mix-blend-mode:difference;transition:transform .15s;}
#curR{width:32px;height:32px;border:1px solid rgba(232,255,71,.5);border-radius:50%;position:fixed;pointer-events:none;z-index:99998;transition:all .12s;}
body:hover #cur{transform:scale(1);}

/* ── PAGES ── */
.page{display:none;min-height:100vh;}
.page.active{display:block;}

/* ════════════════════════════
   NAVBAR
════════════════════════════ */
nav{
  position:fixed;top:0;left:0;right:0;z-index:1000;
  display:flex;align-items:center;justify-content:space-between;
  padding:0 40px;height:64px;
  background:rgba(8,8,8,.92);
  backdrop-filter:blur(24px);
  border-bottom:1px solid var(--border);
}
.nav-logo{
  font-family:'Barlow Condensed',sans-serif;
  font-weight:900;font-size:1.7rem;letter-spacing:6px;
  color:var(--accent);cursor:pointer;
  text-decoration:none;
}
.nav-links{display:flex;gap:0;list-style:none;}
.nav-links a{
  color:var(--muted2);text-decoration:none;
  font-size:.72rem;letter-spacing:2px;text-transform:uppercase;
  font-family:'Space Mono',monospace;
  padding:0 18px;height:64px;
  display:flex;align-items:center;
  transition:color .2s;cursor:pointer;
  border-right:1px solid var(--border);
}
.nav-links a:first-child{border-left:1px solid var(--border);}
.nav-links a:hover,.nav-links a.nav-active{color:var(--accent);}
.nav-right{display:flex;align-items:center;gap:12px;}
.bag-btn{
  background:var(--accent);color:#000;
  border:none;padding:0 20px;height:38px;
  font-family:'Space Mono',monospace;font-size:.72rem;font-weight:700;
  letter-spacing:1px;cursor:pointer;display:flex;align-items:center;gap:8px;
  transition:all .2s;
}
.bag-btn:hover{background:#d4eb3a;}
.bag-count{
  background:#000;color:var(--accent);
  border-radius:50%;width:20px;height:20px;
  font-size:.6rem;display:inline-flex;align-items:center;justify-content:center;
  font-weight:700;
}
.admin-nav-btn{
  background:none;border:1px solid var(--border);color:var(--muted);
  padding:0 16px;height:38px;
  font-family:'Space Mono',monospace;font-size:.65rem;letter-spacing:1px;cursor:pointer;
  transition:all .2s;text-transform:uppercase;
}
.admin-nav-btn:hover{border-color:var(--accent);color:var(--accent);}

/* Mobile hamburger */
.ham{display:none;flex-direction:column;gap:5px;cursor:pointer;background:none;border:none;padding:6px;}
.ham span{width:24px;height:1.5px;background:var(--text);display:block;transition:.3s;}

/* Mobile nav drawer */
.mob-nav{
  position:fixed;top:64px;left:0;right:0;background:var(--surface);
  border-bottom:1px solid var(--border);z-index:999;
  display:none;flex-direction:column;padding:16px 0;
}
.mob-nav.open{display:flex;}
.mob-nav a{
  padding:14px 24px;color:var(--muted2);text-decoration:none;
  font-size:.8rem;letter-spacing:2px;text-transform:uppercase;
  font-family:'Space Mono',monospace;cursor:pointer;
  border-bottom:1px solid var(--border);transition:color .2s;
}
.mob-nav a:hover{color:var(--accent);}

/* ════════════════════════════
   HERO
════════════════════════════ */
.hero{
  min-height:100vh;
  display:grid;grid-template-columns:1fr 1fr;
  padding-top:64px;position:relative;overflow:hidden;
}
.hero-left{
  display:flex;flex-direction:column;justify-content:center;
  padding:80px 60px;position:relative;z-index:2;
}
.hero-right{
  position:relative;overflow:hidden;
  background:var(--surface);
}
.hero-right-img{
  width:100%;height:100%;object-fit:cover;
  display:block;opacity:.85;
}
.hero-right-overlay{
  position:absolute;inset:0;
  background:linear-gradient(to right,var(--bg) 0%,transparent 40%);
}

/* Big stacked text */
.hero-eyebrow{
  font-family:'Space Mono',monospace;
  font-size:.65rem;letter-spacing:4px;color:var(--accent);
  text-transform:uppercase;margin-bottom:20px;
  display:flex;align-items:center;gap:12px;
}
.hero-eyebrow::before{content:'';width:28px;height:1px;background:var(--accent);}
.hero-h1{
  font-family:'Barlow Condensed',sans-serif;
  font-weight:900;
  font-size:clamp(4.5rem,9vw,8.5rem);
  line-height:.88;
  letter-spacing:1px;
  text-transform:uppercase;
  margin-bottom:32px;
}
.hero-h1 .hl{
  color:transparent;
  -webkit-text-stroke:2px var(--accent);
}
.hero-sub{
  font-size:.9rem;color:var(--muted2);line-height:1.7;
  max-width:380px;margin-bottom:44px;
}
.hero-cta-row{display:flex;gap:12px;flex-wrap:wrap;}
.btn-fill{
  background:var(--accent);color:#000;
  padding:14px 32px;border:none;cursor:pointer;
  font-family:'Barlow Condensed',sans-serif;font-weight:700;
  font-size:1rem;letter-spacing:3px;text-transform:uppercase;
  transition:all .25s;
}
.btn-fill:hover{background:#d4eb3a;transform:translateY(-2px);}
.btn-ghost{
  background:transparent;color:var(--text);
  padding:14px 32px;border:1px solid var(--border);cursor:pointer;
  font-family:'Barlow Condensed',sans-serif;font-weight:700;
  font-size:1rem;letter-spacing:3px;text-transform:uppercase;
  transition:all .25s;
}
.btn-ghost:hover{border-color:var(--accent);color:var(--accent);}

/* Hero stats row */
.hero-stats{
  display:flex;gap:32px;margin-top:52px;
  padding-top:32px;border-top:1px solid var(--border);
  flex-wrap:wrap;
}
.hero-stat-num{
  font-family:'Barlow Condensed',sans-serif;
  font-weight:800;font-size:2rem;letter-spacing:1px;color:var(--accent);
}
.hero-stat-lbl{font-size:.7rem;color:var(--muted);letter-spacing:2px;text-transform:uppercase;}

/* Floating badge on image */
.hero-badge{
  position:absolute;bottom:40px;right:40px;
  background:var(--accent);color:#000;
  width:100px;height:100px;border-radius:50%;
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  font-family:'Barlow Condensed',sans-serif;
  font-weight:900;font-size:1rem;letter-spacing:2px;
  text-align:center;line-height:1.1;
  animation:rotateBadge 12s linear infinite;
  z-index:4;
}
@keyframes rotateBadge{from{transform:rotate(0deg);}to{transform:rotate(360deg);}}

/* ════════════════════════════
   TICKER / MARQUEE
════════════════════════════ */
.ticker{
  overflow:hidden;border-top:1px solid var(--border);
  border-bottom:1px solid var(--border);
  background:var(--surface);padding:12px 0;
  position:relative;
}
.ticker-track{
  display:flex;white-space:nowrap;
  animation:ticker 22s linear infinite;
}
.ticker-item{
  font-family:'Barlow Condensed',sans-serif;
  font-weight:700;font-size:1.05rem;letter-spacing:5px;
  color:var(--muted);padding:0 24px;text-transform:uppercase;
}
.ticker-item .dot{color:var(--accent);margin-right:24px;}
@keyframes ticker{from{transform:translateX(0);}to{transform:translateX(-50%);}}

/* ════════════════════════════
   CATEGORY SECTIONS
════════════════════════════ */
.cat-nav{
  display:flex;gap:0;border-bottom:1px solid var(--border);
  overflow-x:auto;scrollbar-width:none;background:var(--surface);
  position:sticky;top:64px;z-index:100;
}
.cat-nav::-webkit-scrollbar{display:none;}
.cat-tab{
  background:none;border:none;border-bottom:2px solid transparent;
  color:var(--muted);padding:0 28px;height:52px;
  font-family:'Space Mono',monospace;font-size:.68rem;
  letter-spacing:2px;text-transform:uppercase;cursor:pointer;
  transition:all .2s;white-space:nowrap;flex-shrink:0;
}
.cat-tab:hover{color:var(--text);}
.cat-tab.active{color:var(--accent);border-bottom-color:var(--accent);}

/* Section title */
.sec-head{
  padding:64px 48px 32px;
  display:flex;align-items:flex-end;justify-content:space-between;
  flex-wrap:wrap;gap:16px;
}
.sec-eyebrow{
  font-family:'Space Mono',monospace;font-size:.62rem;
  letter-spacing:3px;color:var(--accent);
  text-transform:uppercase;margin-bottom:8px;
}
.sec-title{
  font-family:'Barlow Condensed',sans-serif;
  font-weight:900;font-size:clamp(2.2rem,4vw,3.6rem);
  letter-spacing:2px;text-transform:uppercase;line-height:1;
}
.sec-desc{font-size:.82rem;color:var(--muted);margin-top:8px;max-width:400px;}
.view-all-btn{
  background:none;border:1px solid var(--border);color:var(--muted);
  padding:10px 22px;font-family:'Space Mono',monospace;font-size:.65rem;
  letter-spacing:2px;text-transform:uppercase;cursor:pointer;
  transition:all .2s;flex-shrink:0;
}
.view-all-btn:hover{border-color:var(--accent);color:var(--accent);}

/* Category hero banner */
.cat-banner{
  margin:0 48px 32px;
  height:220px;position:relative;overflow:hidden;
  background:var(--surface2);
  display:flex;align-items:center;
}
.cat-banner-bg{
  position:absolute;inset:0;object-fit:cover;width:100%;height:100%;
  opacity:.3;
}
.cat-banner-content{position:relative;z-index:2;padding:0 48px;}
.cat-banner-label{
  font-family:'Space Mono',monospace;font-size:.6rem;letter-spacing:4px;
  color:var(--accent);text-transform:uppercase;margin-bottom:8px;
}
.cat-banner-title{
  font-family:'Barlow Condensed',sans-serif;font-weight:900;
  font-size:3.5rem;letter-spacing:4px;text-transform:uppercase;line-height:1;
}
.cat-banner-sub{font-size:.8rem;color:var(--muted2);margin-top:6px;}

/* ════════════════════════════
   PRODUCT GRID
════════════════════════════ */
.products-grid{
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(260px,1fr));
  gap:2px;padding:0 48px;
}
.product-card{
  background:var(--surface);
  position:relative;overflow:hidden;cursor:pointer;
  transition:transform .3s;
}
.product-card:hover{transform:scale(1.015);z-index:5;}
.p-img-wrap{
  position:relative;width:100%;aspect-ratio:3/4;
  background:var(--surface2);overflow:hidden;
}
.p-img-wrap img{width:100%;height:100%;object-fit:cover;display:block;}
.p-img-placeholder{
  width:100%;height:100%;display:flex;align-items:center;justify-content:center;
  font-family:'Barlow Condensed',sans-serif;font-weight:900;font-size:2rem;
  letter-spacing:6px;color:var(--border);
}
.p-badge{
  position:absolute;top:12px;left:12px;
  font-family:'Space Mono',monospace;font-size:.55rem;font-weight:700;
  letter-spacing:1px;padding:4px 10px;text-transform:uppercase;z-index:2;
}
.p-badge.new-badge{background:var(--accent);color:#000;}
.p-badge.hot-badge{background:var(--red);color:#fff;}
.p-badge.sale-badge{background:var(--red);color:#fff;}
.p-badge.ltd-badge{background:#fff;color:#000;}
.p-overlay{
  position:absolute;inset:0;background:rgba(0,0,0,.75);
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  gap:8px;opacity:0;transition:opacity .25s;
}
.product-card:hover .p-overlay{opacity:1;}
.p-ov-btn{
  background:var(--accent);color:#000;border:none;
  padding:11px 28px;font-family:'Barlow Condensed',sans-serif;
  font-size:.9rem;font-weight:700;letter-spacing:2px;text-transform:uppercase;
  cursor:pointer;width:170px;transition:all .2s;
}
.p-ov-btn:hover{background:#d4eb3a;}
.p-ov-btn.ghost{
  background:transparent;color:#fff;border:1px solid rgba(255,255,255,.3);
}
.p-ov-btn.ghost:hover{border-color:var(--accent);color:var(--accent);}
.p-info{padding:14px 16px;border-top:1px solid var(--border);}
.p-cat-tag{
  font-size:.58rem;letter-spacing:2px;color:var(--muted);
  text-transform:uppercase;font-family:'Space Mono',monospace;margin-bottom:5px;
}
.p-name{font-size:.88rem;font-weight:500;margin-bottom:8px;line-height:1.3;}
.p-meta{display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:4px;}
.p-price{
  font-family:'Barlow Condensed',sans-serif;font-weight:700;
  font-size:1.05rem;color:var(--accent);letter-spacing:1px;
}
.p-price .orig{
  color:var(--muted);text-decoration:line-through;
  font-size:.8rem;margin-right:6px;
}
.p-sizes{
  font-size:.58rem;color:var(--muted);letter-spacing:1px;
  text-transform:uppercase;font-family:'Space Mono',monospace;
}

/* ════════════════════════════
   WOMEN / MEN / ACC BLOCKS
════════════════════════════ */
.gender-blocks{
  display:grid;grid-template-columns:1fr 1fr;gap:2px;
  margin:2px 0;
}
.gender-block{
  position:relative;height:480px;overflow:hidden;cursor:pointer;
}
.gender-block img{
  width:100%;height:100%;object-fit:cover;
  transition:transform .6s;opacity:.6;
}
.gender-block:hover img{transform:scale(1.04);}
.gender-block-overlay{
  position:absolute;inset:0;
  background:linear-gradient(to top,rgba(0,0,0,.85) 0%,transparent 60%);
  display:flex;flex-direction:column;justify-content:flex-end;
  padding:36px;
}
.gb-label{
  font-family:'Space Mono',monospace;font-size:.62rem;letter-spacing:3px;
  color:var(--accent);text-transform:uppercase;margin-bottom:8px;
}
.gb-title{
  font-family:'Barlow Condensed',sans-serif;font-weight:900;
  font-size:3.5rem;letter-spacing:3px;text-transform:uppercase;line-height:1;
  margin-bottom:16px;
}
.gb-btn{
  display:inline-block;
  background:var(--accent);color:#000;padding:10px 24px;
  font-family:'Barlow Condensed',sans-serif;font-weight:700;
  font-size:.95rem;letter-spacing:3px;text-transform:uppercase;
  border:none;cursor:pointer;transition:all .2s;width:fit-content;
}
.gb-btn:hover{background:#d4eb3a;}

/* Accessories grid strip */
.acc-strip{
  display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));
  gap:2px;padding:0 48px;
}

/* ════════════════════════════
   BRAND PERKS STRIP
════════════════════════════ */
.perks{
  display:grid;grid-template-columns:repeat(4,1fr);
  border-top:1px solid var(--border);border-bottom:1px solid var(--border);
  margin:80px 0 0;background:var(--surface);
}
.perk{
  padding:28px 24px;text-align:center;
  border-right:1px solid var(--border);
}
.perk:last-child{border-right:none;}
.perk-icon{font-size:1.6rem;margin-bottom:10px;}
.perk-title{
  font-family:'Barlow Condensed',sans-serif;font-weight:700;
  font-size:1rem;letter-spacing:2px;text-transform:uppercase;
  margin-bottom:4px;
}
.perk-sub{font-size:.72rem;color:var(--muted);letter-spacing:1px;}

/* ════════════════════════════
   CART SIDEBAR
════════════════════════════ */
.cart-overlay{position:fixed;inset:0;background:rgba(0,0,0,.75);z-index:2000;display:none;backdrop-filter:blur(4px);}
.cart-overlay.open{display:block;}
.cart-sidebar{
  position:fixed;top:0;right:-500px;
  width:440px;max-width:95vw;height:100vh;
  background:var(--surface);
  border-left:1px solid var(--border);
  z-index:2001;transition:right .35s cubic-bezier(.4,0,.2,1);
  display:flex;flex-direction:column;
}
.cart-sidebar.open{right:0;}
.cart-head{
  padding:20px 24px;border-bottom:1px solid var(--border);
  display:flex;align-items:center;justify-content:space-between;
}
.cart-hd-title{
  font-family:'Barlow Condensed',sans-serif;font-weight:800;
  font-size:1.5rem;letter-spacing:3px;text-transform:uppercase;
}
.close-x{background:none;border:none;color:var(--muted);font-size:1.4rem;cursor:pointer;transition:color .2s;width:36px;height:36px;display:flex;align-items:center;justify-content:center;}
.close-x:hover{color:var(--red);}
.cart-items{flex:1;overflow-y:auto;padding:16px 20px;}
.cart-empty{text-align:center;padding:70px 20px;color:var(--muted);font-family:'Space Mono',monospace;font-size:.75rem;letter-spacing:2px;line-height:2;}
.cart-item{display:flex;gap:12px;padding:14px 0;border-bottom:1px solid var(--border);position:relative;}
.ci-img{width:72px;height:90px;background:var(--surface2);flex-shrink:0;overflow:hidden;}
.ci-img img{width:100%;height:100%;object-fit:cover;}
.ci-det{flex:1;}
.ci-name{font-size:.84rem;font-weight:500;margin-bottom:3px;}
.ci-size{font-size:.62rem;color:var(--muted);font-family:'Space Mono',monospace;letter-spacing:1px;text-transform:uppercase;margin-bottom:8px;}
.ci-qty{display:flex;align-items:center;gap:8px;}
.qty-b{width:26px;height:26px;background:var(--surface3);border:1px solid var(--border);color:var(--text);cursor:pointer;display:flex;align-items:center;justify-content:center;transition:all .2s;}
.qty-b:hover{border-color:var(--accent);color:var(--accent);}
.qty-n{font-family:'Space Mono',monospace;font-size:.8rem;min-width:20px;text-align:center;}
.ci-price{font-family:'Barlow Condensed',sans-serif;font-weight:700;font-size:1rem;color:var(--accent);align-self:flex-start;white-space:nowrap;}
.rm-item{position:absolute;top:14px;right:0;background:none;border:none;color:var(--muted);cursor:pointer;font-size:.9rem;transition:color .2s;}
.rm-item:hover{color:var(--red);}
.cart-foot{padding:20px 24px;border-top:1px solid var(--border);}
.cart-total-row{display:flex;justify-content:space-between;align-items:center;margin-bottom:6px;}
.cart-total-lbl{font-size:.68rem;font-family:'Space Mono',monospace;letter-spacing:2px;text-transform:uppercase;color:var(--muted);}
.cart-total-val{font-family:'Barlow Condensed',sans-serif;font-weight:800;font-size:1.3rem;color:var(--accent);}
.cod-tag{background:rgba(232,255,71,.1);border:1px solid rgba(232,255,71,.2);padding:8px 12px;margin-bottom:16px;display:flex;align-items:center;gap:8px;}
.cod-tag span{font-size:.65rem;color:var(--accent);font-family:'Space Mono',monospace;letter-spacing:1px;}
.checkout-btn{
  width:100%;background:var(--accent);color:#000;border:none;
  padding:16px;font-family:'Barlow Condensed',sans-serif;
  font-size:1.1rem;font-weight:800;letter-spacing:3px;text-transform:uppercase;cursor:pointer;
  transition:all .25s;
}
.checkout-btn:hover{background:#d4eb3a;}

/* ════════════════════════════
   PRODUCT DETAIL MODAL
════════════════════════════ */
.modal-backdrop{
  position:fixed;inset:0;background:rgba(0,0,0,.9);
  z-index:3000;display:none;align-items:center;justify-content:center;
  padding:20px;backdrop-filter:blur(12px);
}
.modal-backdrop.open{display:flex;}
.modal-box{
  background:var(--surface);
  border:1px solid var(--border);
  width:100%;max-width:820px;
  max-height:92vh;overflow-y:auto;
  position:relative;
}
.pd-wrap{display:grid;grid-template-columns:1fr 1fr;gap:0;}
@media(max-width:640px){.pd-wrap{grid-template-columns:1fr;}}
.pd-img-side{aspect-ratio:3/4;background:var(--surface2);overflow:hidden;}
.pd-img-side img{width:100%;height:100%;object-fit:cover;display:block;}
.pd-info-side{padding:36px;display:flex;flex-direction:column;justify-content:center;}
.pd-category{font-family:'Space Mono',monospace;font-size:.6rem;letter-spacing:3px;color:var(--accent);text-transform:uppercase;margin-bottom:8px;}
.pd-name{font-family:'Barlow Condensed',sans-serif;font-weight:900;font-size:2rem;letter-spacing:2px;text-transform:uppercase;margin-bottom:12px;line-height:1.1;}
.pd-price{font-family:'Barlow Condensed',sans-serif;font-weight:800;font-size:1.6rem;color:var(--accent);margin-bottom:20px;}
.pd-price .orig{color:var(--muted);text-decoration:line-through;font-size:1rem;margin-right:8px;}
.pd-desc{font-size:.83rem;color:var(--muted2);line-height:1.7;margin-bottom:24px;}
.sz-label{font-family:'Space Mono',monospace;font-size:.6rem;letter-spacing:2px;text-transform:uppercase;color:var(--muted);margin-bottom:8px;}
.sz-opts{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:24px;}
.sz-opt{
  width:42px;height:42px;background:var(--surface2);border:1px solid var(--border);
  color:var(--text);cursor:pointer;font-family:'Space Mono',monospace;font-size:.68rem;
  font-weight:700;transition:all .2s;display:flex;align-items:center;justify-content:center;
}
.sz-opt:hover{border-color:var(--accent);color:var(--accent);}
.sz-opt.sel{background:var(--accent);color:#000;border-color:var(--accent);}
.pd-add-btn{
  width:100%;background:var(--accent);color:#000;border:none;
  padding:14px;font-family:'Barlow Condensed',sans-serif;
  font-size:1rem;font-weight:800;letter-spacing:3px;cursor:pointer;
  text-transform:uppercase;transition:all .25s;margin-bottom:8px;
}
.pd-add-btn:hover{background:#d4eb3a;}
.pd-buy-btn{
  width:100%;background:transparent;color:var(--text);
  border:1px solid var(--border);padding:12px;
  font-family:'Barlow Condensed',sans-serif;font-size:.95rem;font-weight:700;
  letter-spacing:3px;cursor:pointer;text-transform:uppercase;transition:all .25s;
}
.pd-buy-btn:hover{border-color:var(--accent);color:var(--accent);}
.modal-close-btn{
  position:absolute;top:16px;right:16px;
  background:var(--surface3);border:1px solid var(--border);color:var(--muted);
  width:36px;height:36px;cursor:pointer;font-size:1.1rem;
  display:flex;align-items:center;justify-content:center;
  transition:all .2s;z-index:2;
}
.modal-close-btn:hover{background:var(--red);border-color:var(--red);color:#fff;}

/* ════════════════════════════
   CHECKOUT MODAL
════════════════════════════ */
#checkoutModal{z-index:4000;}
.chk-box{
  background:var(--surface);border:1px solid var(--border);
  width:100%;max-width:540px;max-height:92vh;overflow-y:auto;
  position:relative;
}
.chk-head{
  padding:20px 24px;border-bottom:1px solid var(--border);
  display:flex;align-items:center;justify-content:space-between;
  position:sticky;top:0;background:var(--surface);z-index:1;
}
.chk-title{font-family:'Barlow Condensed',sans-serif;font-weight:800;font-size:1.5rem;letter-spacing:3px;}
.chk-body{padding:28px;}
.f-group{margin-bottom:16px;}
.f-label{
  display:block;font-size:.62rem;font-family:'Space Mono',monospace;
  letter-spacing:2px;text-transform:uppercase;color:var(--muted);margin-bottom:6px;
}
.f-input,.f-select{
  width:100%;background:var(--surface2);border:1px solid var(--border);
  color:var(--text);padding:11px 13px;font-family:'Inter',sans-serif;
  font-size:.88rem;outline:none;transition:border-color .2s;-webkit-appearance:none;
}
.f-input:focus,.f-select:focus{border-color:var(--accent);}
.f-row{display:grid;grid-template-columns:1fr 1fr;gap:12px;}
.order-sum{background:var(--surface2);border:1px solid var(--border);padding:16px;margin-bottom:20px;}
.order-sum-title{font-family:'Space Mono',monospace;font-size:.62rem;letter-spacing:2px;text-transform:uppercase;color:var(--muted);margin-bottom:10px;}
.o-row{display:flex;justify-content:space-between;font-size:.83rem;margin-bottom:5px;}
.o-row.total{border-top:1px solid var(--border);margin-top:8px;padding-top:8px;font-weight:700;font-family:'Barlow Condensed',sans-serif;font-size:1.1rem;color:var(--accent);}

/* COD box */
.cod-box{
  background:linear-gradient(135deg,#121a00,#0a1200);
  border:1px solid rgba(232,255,71,.3);
  padding:18px;margin-bottom:20px;
  display:flex;align-items:center;gap:14px;
}
.cod-icon{font-size:1.8rem;flex-shrink:0;}
.cod-text-title{
  font-family:'Barlow Condensed',sans-serif;font-weight:800;
  font-size:1.1rem;letter-spacing:2px;color:var(--accent);
}
.cod-text-sub{font-size:.72rem;color:var(--muted2);line-height:1.5;margin-top:2px;}
.place-order-btn{
  width:100%;background:var(--accent);color:#000;border:none;
  padding:18px;font-family:'Barlow Condensed',sans-serif;
  font-size:1.15rem;font-weight:900;letter-spacing:4px;text-transform:uppercase;
  cursor:pointer;transition:all .25s;display:flex;align-items:center;justify-content:center;gap:10px;
}
.place-order-btn:hover{background:#d4eb3a;transform:translateY(-1px);}

/* Success */
.success-wrap{text-align:center;padding:60px 32px;}
.success-circle{
  width:80px;height:80px;background:var(--accent);border-radius:50%;
  display:flex;align-items:center;justify-content:center;
  margin:0 auto 24px;font-size:2rem;color:#000;
}
.success-title{font-family:'Barlow Condensed',sans-serif;font-weight:900;font-size:2.6rem;letter-spacing:3px;margin-bottom:12px;}
.success-msg{color:var(--muted2);font-size:.88rem;line-height:1.7;margin-bottom:24px;}
.success-oid{font-family:'Space Mono',monospace;font-size:.68rem;letter-spacing:2px;color:var(--accent);margin-bottom:30px;}

/* ════════════════════════════
   TOAST
════════════════════════════ */
.toast{
  position:fixed;bottom:28px;left:50%;
  transform:translateX(-50%) translateY(80px);
  background:var(--accent);color:#000;
  padding:12px 28px;font-family:'Space Mono',monospace;
  font-size:.72rem;font-weight:700;letter-spacing:1px;
  z-index:9999;transition:transform .4s cubic-bezier(.4,0,.2,1);
  pointer-events:none;
}
.toast.show{transform:translateX(-50%) translateY(0);}

/* ════════════════════════════
   FOOTER
════════════════════════════ */
footer{
  background:var(--surface);border-top:1px solid var(--border);
  padding:52px 48px 28px;
}
.foot-grid{display:grid;grid-template-columns:2fr 1fr 1fr;gap:40px;margin-bottom:40px;}
.foot-brand{font-family:'Barlow Condensed',sans-serif;font-weight:900;font-size:2.8rem;letter-spacing:6px;color:var(--accent);margin-bottom:12px;}
.foot-desc{font-size:.8rem;color:var(--muted);line-height:1.7;max-width:260px;}
.foot-head{font-family:'Space Mono',monospace;font-size:.6rem;letter-spacing:3px;text-transform:uppercase;color:var(--text);margin-bottom:14px;}
.foot-links{list-style:none;}
.foot-links li{margin-bottom:9px;}
.foot-links a{color:var(--muted);text-decoration:none;font-size:.8rem;transition:color .2s;cursor:pointer;}
.foot-links a:hover{color:var(--accent);}
.foot-bottom{border-top:1px solid var(--border);padding-top:20px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px;}
.foot-copy{font-size:.68rem;color:var(--muted);font-family:'Space Mono',monospace;}

/* ════════════════════════════
   ADMIN - LOGIN
════════════════════════════ */
.login-page{min-height:100vh;background:var(--bg);display:flex;align-items:center;justify-content:center;padding:20px;}
.login-box{background:var(--surface);border:1px solid var(--border);padding:44px;width:100%;max-width:380px;}
.login-logo{font-family:'Barlow Condensed',sans-serif;font-weight:900;font-size:2.4rem;letter-spacing:6px;color:var(--accent);margin-bottom:4px;}
.login-sub{font-size:.65rem;color:var(--muted);font-family:'Space Mono',monospace;letter-spacing:3px;text-transform:uppercase;margin-bottom:32px;}
.login-btn{width:100%;background:var(--accent);color:#000;border:none;padding:14px;font-family:'Barlow Condensed',sans-serif;font-size:1rem;font-weight:800;letter-spacing:3px;cursor:pointer;transition:all .25s;text-transform:uppercase;margin-top:8px;}
.login-btn:hover{background:#d4eb3a;}
.login-err{color:var(--red);font-size:.72rem;margin-top:8px;text-align:center;font-family:'Space Mono',monospace;display:none;}
.login-err.show{display:block;}

/* ════════════════════════════
   ADMIN PANEL
════════════════════════════ */
.admin-pg{min-height:100vh;background:var(--bg);}
.admin-top{
  background:var(--surface);border-bottom:1px solid var(--border);
  padding:0 28px;height:60px;
  display:flex;align-items:center;justify-content:space-between;
}
.admin-brand{font-family:'Barlow Condensed',sans-serif;font-weight:800;font-size:1.4rem;letter-spacing:4px;}
.admin-brand span{color:var(--accent);}
.adm-btn{background:none;border:1px solid var(--border);color:var(--muted);padding:7px 14px;font-family:'Space Mono',monospace;font-size:.6rem;letter-spacing:1px;cursor:pointer;transition:all .2s;text-transform:uppercase;}
.adm-btn:hover{border-color:var(--accent);color:var(--accent);}
.adm-btn.logout:hover{border-color:var(--red);color:var(--red);}
.admin-layout{display:grid;grid-template-columns:200px 1fr;min-height:calc(100vh - 60px);}
.admin-side{background:var(--surface);border-right:1px solid var(--border);padding:16px 0;}
.adm-menu-item{
  display:flex;align-items:center;gap:10px;padding:11px 18px;
  cursor:pointer;font-size:.78rem;color:var(--muted);letter-spacing:.5px;
  transition:all .2s;border-left:2px solid transparent;
}
.adm-menu-item:hover{color:var(--text);background:rgba(255,255,255,.02);}
.adm-menu-item.active{color:var(--accent);border-left-color:var(--accent);background:rgba(232,255,71,.05);}
.adm-menu-icon{font-size:.95rem;}
.admin-main{padding:28px;}
.adm-section{display:none;}
.adm-section.active{display:block;}
.adm-title{font-family:'Barlow Condensed',sans-serif;font-weight:900;font-size:2.2rem;letter-spacing:3px;margin-bottom:4px;text-transform:uppercase;}
.adm-sub{color:var(--muted);font-size:.8rem;margin-bottom:26px;}

/* Stats */
.stats-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:14px;margin-bottom:28px;}
.stat-card{background:var(--surface);border:1px solid var(--border);padding:18px;}
.stat-lbl{font-family:'Space Mono',monospace;font-size:.58rem;letter-spacing:2px;text-transform:uppercase;color:var(--muted);margin-bottom:6px;}
.stat-val{font-family:'Barlow Condensed',sans-serif;font-weight:900;font-size:2.2rem;color:var(--accent);letter-spacing:1px;}
.stat-note{font-size:.68rem;color:var(--muted);margin-top:3px;}

/* Products admin */
.adm-form{background:var(--surface);border:1px solid var(--border);padding:24px;margin-bottom:24px;}
.adm-form-title{font-family:'Barlow Condensed',sans-serif;font-weight:800;font-size:1.3rem;letter-spacing:2px;margin-bottom:18px;padding-bottom:12px;border-bottom:1px solid var(--border);text-transform:uppercase;}
.adm-form-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;}
.adm-form-full{grid-column:1/-1;}
.adm-form textarea{width:100%;background:var(--surface2);border:1px solid var(--border);color:var(--text);padding:11px 13px;font-family:'Inter',sans-serif;font-size:.88rem;resize:vertical;min-height:80px;outline:none;transition:border-color .2s;}
.adm-form textarea:focus{border-color:var(--accent);}
.adm-save{background:var(--accent);color:#000;border:none;padding:11px 28px;font-family:'Barlow Condensed',sans-serif;font-size:.9rem;font-weight:800;letter-spacing:2px;cursor:pointer;transition:all .2s;margin-top:8px;text-transform:uppercase;}
.adm-save:hover{background:#d4eb3a;}
.adm-cancel{background:transparent;color:var(--muted);border:1px solid var(--border);padding:11px 20px;font-family:'Barlow Condensed',sans-serif;font-size:.9rem;font-weight:700;letter-spacing:2px;cursor:pointer;transition:all .2s;margin-top:8px;margin-left:8px;text-transform:uppercase;}
.adm-cancel:hover{border-color:var(--muted);color:var(--text);}
.add-prod-btn{background:var(--accent);color:#000;border:none;padding:9px 20px;font-family:'Space Mono',monospace;font-size:.62rem;font-weight:700;letter-spacing:1px;cursor:pointer;transition:all .2s;text-transform:uppercase;}
.add-prod-btn:hover{background:#d4eb3a;}
.prods-table-wrap{background:var(--surface);border:1px solid var(--border);overflow:hidden;}
.tbl-head{display:flex;align-items:center;justify-content:space-between;padding:16px 18px;border-bottom:1px solid var(--border);}
.tbl-title{font-family:'Space Mono',monospace;font-size:.65rem;letter-spacing:2px;text-transform:uppercase;}
.t-scroll{overflow-x:auto;}
table{width:100%;border-collapse:collapse;}
thead tr{border-bottom:1px solid var(--border);background:var(--surface2);}
th,td{padding:11px 14px;text-align:left;font-size:.76rem;}
th{font-family:'Space Mono',monospace;font-size:.58rem;letter-spacing:2px;text-transform:uppercase;color:var(--muted);white-space:nowrap;}
td{border-bottom:1px solid rgba(40,40,40,.5);}
tr:last-child td{border-bottom:none;}
.td-img{width:40px;height:50px;background:var(--surface2);overflow:hidden;display:block;}
.td-img img{width:100%;height:100%;object-fit:cover;}
.td-bdg{font-family:'Space Mono',monospace;font-size:.55rem;padding:2px 7px;text-transform:uppercase;letter-spacing:1px;display:inline-block;}
.td-bdg.on{background:rgba(232,255,71,.15);color:var(--accent);}
.td-bdg.off{background:rgba(100,100,100,.15);color:var(--muted);}
.t-action{background:none;border:none;cursor:pointer;color:var(--muted);font-size:.72rem;font-family:'Space Mono',monospace;padding:4px 8px;transition:color .2s;text-decoration:underline;}
.t-action:hover{color:var(--accent);}
.t-action.del:hover{color:var(--red);}

/* Orders admin */
.o-row-item{background:var(--surface);border:1px solid var(--border);padding:14px 18px;margin-bottom:8px;display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:10px;}
.o-info{flex:1;min-width:200px;}
.o-num{font-family:'Space Mono',monospace;font-size:.72rem;color:var(--accent);margin-bottom:3px;}
.o-customer{font-size:.83rem;margin-bottom:2px;}
.o-date{font-size:.68rem;color:var(--muted);}
.o-amount{font-family:'Barlow Condensed',sans-serif;font-weight:800;font-size:1.1rem;color:var(--accent);}
.o-status-sel{background:var(--surface2);border:1px solid var(--border);color:var(--text);padding:5px 9px;font-family:'Space Mono',monospace;font-size:.62rem;cursor:pointer;outline:none;}
.settings-block{background:var(--surface);border:1px solid var(--border);padding:22px;margin-bottom:18px;}
.settings-h{font-family:'Barlow Condensed',sans-serif;font-weight:800;font-size:1.2rem;letter-spacing:2px;margin-bottom:16px;padding-bottom:10px;border-bottom:1px solid var(--border);text-transform:uppercase;}
.notif-dot{width:7px;height:7px;background:var(--red);border-radius:50%;display:inline-block;margin-left:5px;animation:blink 1.5s infinite;}
@keyframes blink{0%,100%{opacity:1;}50%{opacity:.2;}}

/* ════════════════════════════
   RESPONSIVE
════════════════════════════ */
@media(max-width:900px){
  .hero{grid-template-columns:1fr;}
  .hero-right{display:none;}
  .hero-left{padding:60px 28px;}
  .gender-blocks{grid-template-columns:1fr;}
  .perks{grid-template-columns:1fr 1fr;}
  .foot-grid{grid-template-columns:1fr;}
  .admin-layout{grid-template-columns:1fr;}
  .admin-side{display:none;}
  .adm-form-grid{grid-template-columns:1fr;}
}
@media(max-width:768px){
  nav{padding:0 20px;}
  .nav-links{display:none;}
  .ham{display:flex;}
  .sec-head{padding:48px 20px 24px;}
  .products-grid{padding:0 12px;grid-template-columns:repeat(2,1fr);}
  .acc-strip{padding:0 12px;}
  .cat-banner{margin:0 12px 24px;}
  footer{padding:40px 20px 24px;}
  .perks{grid-template-columns:1fr 1fr;}
  .products-grid{gap:1px;}
  .gender-blocks{gap:1px;}
  .cat-banner{height:160px;}
  .gb-title{font-size:2.5rem;}
}
@media(max-width:480px){
  .products-grid{grid-template-columns:1fr;}
  .perks{grid-template-columns:1fr;}
  .perk:last-child{border-bottom:none;}
  .perk{border-right:none;border-bottom:1px solid var(--border);}
  .pd-info-side{padding:20px;}
  .pd-name{font-size:1.6rem;}
}
</style>
</head>
<body>

<!-- Cursor -->
<div id="cur"></div>
<div id="curR"></div>
<!-- Toast -->
<div class="toast" id="toast"></div>

<!-- ══════════════════════════════════════
  STORE PAGE
══════════════════════════════════════ -->
<div class="page active" id="storePage">

  <!-- NAVBAR -->
  <nav>
    <div class="nav-logo" onclick="scrollTo(0,0)">SANJHEX</div>
    <ul class="nav-links" id="navLinks">
      <li><a onclick="showCat('all')" class="nav-active" id="navAll">ALL</a></li>
      <li><a onclick="showCat('women')" id="navWomen">WOMEN</a></li>
      <li><a onclick="showCat('men')" id="navMen">MEN</a></li>
      <li><a onclick="showCat('accessories')" id="navAcc">ACCESSORIES</a></li>
    </ul>
    <div class="nav-right">
      <button class="bag-btn" onclick="openCart()">BAG <span class="bag-count" id="cartCount">0</span></button>
      <button class="admin-nav-btn" onclick="showPage('loginPage')">ADMIN</button>
      <button class="ham" id="hamBtn" onclick="toggleMob()" aria-label="menu">
        <span></span><span></span><span></span>
      </button>
    </div>
  </nav>

  <!-- MOBILE NAV -->
  <div class="mob-nav" id="mobNav">
    <a onclick="showCat('all');toggleMob()">ALL</a>
    <a onclick="showCat('women');toggleMob()">WOMEN</a>
    <a onclick="showCat('men');toggleMob()">MEN</a>
    <a onclick="showCat('accessories');toggleMob()">ACCESSORIES</a>
    <a onclick="showPage('loginPage');toggleMob()">ADMIN LOGIN</a>
  </div>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-left">
      <div class="hero-eyebrow">New Drop — SS2025</div>
      <h1 class="hero-h1">WEAR<br>YOUR<br><span class="hl">TRUTH</span></h1>
      <p class="hero-sub">Unisex streetwear for those who exist beyond labels. Bold silhouettes, raw textures — made to be worn, not admired.</p>
      <div class="hero-cta-row">
        <button class="btn-fill" onclick="scrollToCats()">SHOP NOW</button>
        <button class="btn-ghost" onclick="showCat('women');scrollToCats()">WOMENS</button>
        <button class="btn-ghost" onclick="showCat('men');scrollToCats()">MENS</button>
      </div>
      <div class="hero-stats">
        <div>
          <div class="hero-stat-num">100%</div>
          <div class="hero-stat-lbl">Unisex Fits</div>
        </div>
        <div>
          <div class="hero-stat-num">COD</div>
          <div class="hero-stat-lbl">Pay on Delivery</div>
        </div>
        <div>
          <div class="hero-stat-num">3–5</div>
          <div class="hero-stat-lbl">Day Dispatch</div>
        </div>
      </div>
    </div>
    <div class="hero-right">
      <img class="hero-right-img" src="https://images.unsplash.com/photo-1487412720507-e7ab37603c6f?w=900&q=80" alt="SANJHEX">
      <div class="hero-right-overlay"></div>
      <div class="hero-badge">NEW<br>DROP</div>
    </div>
  </section>

  <!-- TICKER -->
  <div class="ticker">
    <div class="ticker-track" id="tickerTrack">
      <span class="ticker-item"><span class="dot">✦</span>SANJHEX</span>
      <span class="ticker-item"><span class="dot">✦</span>UNISEX STREETWEAR</span>
      <span class="ticker-item"><span class="dot">✦</span>CASH ON DELIVERY</span>
      <span class="ticker-item"><span class="dot">✦</span>FREE RETURNS 7 DAYS</span>
      <span class="ticker-item"><span class="dot">✦</span>SS 2025 DROP</span>
      <span class="ticker-item"><span class="dot">✦</span>MADE FOR ALL</span>
      <span class="ticker-item"><span class="dot">✦</span>SANJHEX</span>
      <span class="ticker-item"><span class="dot">✦</span>UNISEX STREETWEAR</span>
      <span class="ticker-item"><span class="dot">✦</span>CASH ON DELIVERY</span>
      <span class="ticker-item"><span class="dot">✦</span>FREE RETURNS 7 DAYS</span>
      <span class="ticker-item"><span class="dot">✦</span>SS 2025 DROP</span>
      <span class="ticker-item"><span class="dot">✦</span>MADE FOR ALL</span>
    </div>
  </div>

  <!-- GENDER BLOCKS -->
  <div class="gender-blocks" id="catsSection">
    <div class="gender-block" onclick="showCat('women');scrollToCats()">
      <img src="https://images.unsplash.com/photo-1509631179647-0177331693ae?w=900&q=80" alt="Women">
      <div class="gender-block-overlay">
        <div class="gb-label">Shop</div>
        <div class="gb-title">WOMEN<br>WEAR</div>
        <button class="gb-btn" onclick="event.stopPropagation();showCat('women');scrollToProducts()">SHOP NOW</button>
      </div>
    </div>
    <div class="gender-block" onclick="showCat('men');scrollToCats()">
      <img src="https://images.unsplash.com/photo-1519085360753-af0119f7cbe7?w=900&q=80" alt="Men">
      <div class="gender-block-overlay">
        <div class="gb-label">Shop</div>
        <div class="gb-title">MEN<br>WEAR</div>
        <button class="gb-btn" onclick="event.stopPropagation();showCat('men');scrollToProducts()">SHOP NOW</button>
      </div>
    </div>
  </div>

  <!-- ─── MAIN SHOP SECTION ─── -->
  <div id="productsSection">

    <!-- Category nav tabs -->
    <div class="cat-nav" id="catNav">
      <button class="cat-tab active" id="tabAll" onclick="showCat('all')">ALL DROPS</button>
      <button class="cat-tab" id="tabWomen" onclick="showCat('women')">WOMEN WEAR</button>
      <button class="cat-tab" id="tabMen" onclick="showCat('men')">MEN WEAR</button>
      <button class="cat-tab" id="tabAcc" onclick="showCat('accessories')">ACCESSORIES</button>
      <button class="cat-tab" id="tabUpper" onclick="showCat('upper')">UPPER WEAR</button>
      <button class="cat-tab" id="tabBottom" onclick="showCat('bottom')">BOTTOM WEAR</button>
      <button class="cat-tab" id="tabNew" onclick="showCat('new')">NEW IN</button>
    </div>

    <!-- Section Header -->
    <div class="sec-head">
      <div>
        <div class="sec-eyebrow" id="secEyebrow">The Collection</div>
        <div class="sec-title" id="secTitle">ALL DROPS</div>
        <div class="sec-desc" id="secDesc">Every piece, every drop — all in one place.</div>
      </div>
      <button class="view-all-btn" onclick="showCat('all')">VIEW ALL</button>
    </div>

    <!-- Products Grid -->
    <div class="products-grid" id="productsGrid"></div>
    <div id="emptyState" style="display:none;text-align:center;padding:80px 20px;color:var(--muted);font-family:'Space Mono',monospace;font-size:.75rem;letter-spacing:3px;">NO PRODUCTS IN THIS CATEGORY YET</div>

  </div>

  <!-- PERKS -->
  <div class="perks">
    <div class="perk">
      <div class="perk-icon">📦</div>
      <div class="perk-title">Cash on Delivery</div>
      <div class="perk-sub">Pay when it arrives</div>
    </div>
    <div class="perk">
      <div class="perk-icon">🚚</div>
      <div class="perk-title">3–5 Day Dispatch</div>
      <div class="perk-sub">Pan-India delivery</div>
    </div>
    <div class="perk">
      <div class="perk-icon">↩️</div>
      <div class="perk-title">7-Day Returns</div>
      <div class="perk-sub">Hassle-free policy</div>
    </div>
    <div class="perk">
      <div class="perk-icon">✦</div>
      <div class="perk-title">100% Unisex</div>
      <div class="perk-sub">Made for everyone</div>
    </div>
  </div>

  <!-- FOOTER -->
  <footer>
    <div class="foot-grid">
      <div>
        <div class="foot-brand">SANJHEX</div>
        <div class="foot-desc">Unisex streetwear for the ones who exist beyond labels. Drop-based collections, limited pieces. Made with intent.</div>
      </div>
      <div>
        <div class="foot-head">Quick Links</div>
        <ul class="foot-links">
          <li><a onclick="showCat('all');scrollToProducts()">Shop All</a></li>
          <li><a onclick="showCat('women');scrollToProducts()">Women Wear</a></li>
          <li><a onclick="showCat('men');scrollToProducts()">Men Wear</a></li>
          <li><a onclick="showCat('accessories');scrollToProducts()">Accessories</a></li>
          <li><a onclick="showCat('new');scrollToProducts()">New In</a></li>
        </ul>
      </div>
      <div>
        <div class="foot-head">Help</div>
        <ul class="foot-links">
          <li><a href="#">Sizing Guide</a></li>
          <li><a href="#">Shipping Info</a></li>
          <li><a href="#">Returns Policy</a></li>
          <li><a href="mailto:sanjhex@gmail.com">Contact Us</a></li>
        </ul>
      </div>
    </div>
    <div class="foot-bottom">
      <div class="foot-copy">© 2025 SANJHEX. All rights reserved.</div>
      <div class="foot-copy">Made with ✦ in India</div>
    </div>
  </footer>
</div><!-- /storePage -->

<!-- ══════════════════════════════════════
  CART SIDEBAR
══════════════════════════════════════ -->
<div class="cart-overlay" id="cartOverlay" onclick="closeCart()"></div>
<div class="cart-sidebar" id="cartSidebar">
  <div class="cart-head">
    <div class="cart-hd-title">YOUR BAG</div>
    <button class="close-x" onclick="closeCart()">✕</button>
  </div>
  <div class="cart-items" id="cartItems">
    <div class="cart-empty" id="cartEmpty">YOUR BAG IS EMPTY<br><br>Start shopping →</div>
  </div>
  <div class="cart-foot" id="cartFoot" style="display:none;">
    <div class="cart-total-row">
      <span class="cart-total-lbl">Subtotal</span>
      <span class="cart-total-val" id="cartTotal">₹0</span>
    </div>
    <div class="cod-tag">
      <span>💵 CASH ON DELIVERY — No prepayment needed</span>
    </div>
    <button class="checkout-btn" onclick="openCheckout()">CHECKOUT — COD</button>
  </div>
</div>

<!-- ══════════════════════════════════════
  PRODUCT DETAIL MODAL
══════════════════════════════════════ -->
<div class="modal-backdrop" id="productModal">
  <div class="modal-box">
    <button class="modal-close-btn" onclick="closePD()">✕</button>
    <div class="pd-wrap">
      <div class="pd-img-side">
        <img id="pdImg" src="" alt="" onerror="this.style.display='none'">
      </div>
      <div class="pd-info-side">
        <div class="pd-category" id="pdCat"></div>
        <div class="pd-name" id="pdName"></div>
        <div class="pd-price" id="pdPrice"></div>
        <div class="pd-desc" id="pdDesc"></div>
        <div class="sz-label">SELECT SIZE</div>
        <div class="sz-opts" id="pdSizes"></div>
        <button class="pd-add-btn" onclick="pdAddToCart()">ADD TO BAG</button>
        <button class="pd-buy-btn" onclick="pdBuyNow()">BUY NOW</button>
      </div>
    </div>
  </div>
</div>

<!-- ══════════════════════════════════════
  CHECKOUT MODAL
══════════════════════════════════════ -->
<div class="modal-backdrop" id="checkoutModal">
  <div class="chk-box">
    <div class="chk-head">
      <div class="chk-title">CHECKOUT</div>
      <button class="close-x" onclick="closeCheckout()">✕</button>
    </div>
    <div class="chk-body" id="chkBody">
      <!-- Order Summary -->
      <div class="order-sum" id="chkSummary"></div>

      <!-- COD Badge -->
      <div class="cod-box">
        <div class="cod-icon">💵</div>
        <div>
          <div class="cod-text-title">CASH ON DELIVERY</div>
          <div class="cod-text-sub">Pay when your order arrives at your door. No online payment required.</div>
        </div>
      </div>

      <!-- Form -->
      <div class="f-group">
        <label class="f-label">Full Name</label>
        <input class="f-input" id="cName" placeholder="Your full name" type="text">
      </div>
      <div class="f-row">
        <div class="f-group">
          <label class="f-label">Phone</label>
          <input class="f-input" id="cPhone" placeholder="10-digit number" type="tel">
        </div>
        <div class="f-group">
          <label class="f-label">Email (Optional)</label>
          <input class="f-input" id="cEmail" placeholder="email@example.com" type="email">
        </div>
      </div>
      <div class="f-group">
        <label class="f-label">Address</label>
        <input class="f-input" id="cAddress" placeholder="Flat, Street, Area" type="text">
      </div>
      <div class="f-row">
        <div class="f-group">
          <label class="f-label">City</label>
          <input class="f-input" id="cCity" placeholder="City" type="text">
        </div>
        <div class="f-group">
          <label class="f-label">PIN Code</label>
          <input class="f-input" id="cPin" placeholder="6-digit PIN" type="text">
        </div>
      </div>
      <div class="f-group">
        <label class="f-label">State</label>
        <select class="f-select" id="cState">
          <option value="">Select State</option>
          <option>Andhra Pradesh</option><option>Assam</option><option>Bihar</option>
          <option>Delhi</option><option>Goa</option><option>Gujarat</option>
          <option>Haryana</option><option>Himachal Pradesh</option><option>Jharkhand</option>
          <option>Karnataka</option><option>Kerala</option><option>Madhya Pradesh</option>
          <option>Maharashtra</option><option>Manipur</option><option>Meghalaya</option>
          <option>Odisha</option><option>Punjab</option><option>Rajasthan</option>
          <option>Tamil Nadu</option><option>Telangana</option><option>Uttar Pradesh</option>
          <option>Uttarakhand</option><option>West Bengal</option><option>Other</option>
        </select>
      </div>

      <button class="place-order-btn" onclick="placeOrder()">✦ PLACE ORDER — COD</button>
    </div>

    <!-- Success -->
    <div class="success-wrap" id="successWrap" style="display:none;">
      <div class="success-circle">✓</div>
      <div class="success-title">ORDER PLACED!</div>
      <div class="success-msg">Thank you! Your COD order is confirmed. We'll call/text you with the tracking details soon.</div>
      <div class="success-oid" id="successOid"></div>
      <button class="btn-fill" onclick="closeCheckout();clearCart();">CONTINUE SHOPPING</button>
    </div>
  </div>
</div>

<!-- ══════════════════════════════════════
  LOGIN PAGE
══════════════════════════════════════ -->
<div class="page" id="loginPage">
  <div class="login-page">
    <div class="login-box">
      <div class="login-logo">SANJHEX</div>
      <div class="login-sub">Admin Panel</div>
      <div class="f-group">
        <label class="f-label">Username</label>
        <input class="f-input" id="loginUser" placeholder="admin" type="text">
      </div>
      <div class="f-group">
        <label class="f-label">Password</label>
        <input class="f-input" id="loginPass" placeholder="••••••••" type="password" onkeydown="if(event.key==='Enter')adminLogin()">
      </div>
      <button class="login-btn" onclick="adminLogin()">LOGIN</button>
      <div class="login-err" id="loginErr">Invalid credentials.</div>
      <div style="margin-top:20px;text-align:center;">
        <button onclick="showPage('storePage')" style="background:none;border:none;color:var(--muted);font-size:.68rem;cursor:pointer;font-family:'Space Mono',monospace;letter-spacing:1px;">← BACK TO STORE</button>
      </div>
    </div>
  </div>
</div>

<!-- ══════════════════════════════════════
  ADMIN PAGE
══════════════════════════════════════ -->
<div class="page" id="adminPage">
  <div class="admin-pg">
    <div class="admin-top">
      <div class="admin-brand">SANJHEX <span>ADMIN</span></div>
      <div style="display:flex;gap:10px;align-items:center;">
        <button class="adm-btn" onclick="showPage('storePage')">VIEW STORE</button>
        <button class="adm-btn logout" onclick="adminLogout()">LOGOUT</button>
      </div>
    </div>

    <div class="admin-layout">
      <!-- Sidebar -->
      <div class="admin-side">
        <div class="adm-menu-item active" onclick="switchAdmin('dashboard',this)">
          <span class="adm-menu-icon">◈</span> Dashboard
        </div>
        <div class="adm-menu-item" onclick="switchAdmin('products',this)">
          <span class="adm-menu-icon">◉</span> Products
        </div>
        <div class="adm-menu-item" onclick="switchAdmin('orders',this)">
          <span class="adm-menu-icon">◎</span> Orders <span class="notif-dot" id="orderDot"></span>
        </div>
        <div class="adm-menu-item" onclick="switchAdmin('settings',this)">
          <span class="adm-menu-icon">◈</span> Settings
        </div>
      </div>

      <!-- Main content -->
      <div class="admin-main">

        <!-- Dashboard -->
        <div class="adm-section active" id="admin-dashboard">
          <div class="adm-title">DASHBOARD</div>
          <div class="adm-sub">Your store at a glance</div>
          <div class="stats-grid">
            <div class="stat-card"><div class="stat-lbl">Total Products</div><div class="stat-val" id="statProducts">0</div><div class="stat-note">Active in store</div></div>
            <div class="stat-card"><div class="stat-lbl">Total Orders</div><div class="stat-val" id="statOrders">0</div><div class="stat-note">All time</div></div>
            <div class="stat-card"><div class="stat-lbl">Revenue (COD)</div><div class="stat-val" id="statRevenue">₹0</div><div class="stat-note">All time</div></div>
            <div class="stat-card"><div class="stat-lbl">Pending Orders</div><div class="stat-val" id="statPending">0</div><div class="stat-note">Need action</div></div>
          </div>
          <div style="background:var(--surface);border:1px solid var(--border);padding:18px;">
            <div style="font-family:'Space Mono',monospace;font-size:.6rem;letter-spacing:2px;text-transform:uppercase;color:var(--muted);margin-bottom:12px;">Recent Orders</div>
            <div id="dashboardOrders"></div>
          </div>
        </div>

        <!-- Products -->
        <div class="adm-section" id="admin-products">
          <div class="adm-title">PRODUCTS</div>
          <div class="adm-sub">Manage your store inventory</div>

          <!-- Add/Edit Form -->
          <div class="adm-form" id="productForm" style="display:none;">
            <div class="adm-form-title" id="formTitle">ADD NEW PRODUCT</div>
            <input type="hidden" id="editProductId">
            <div class="adm-form-grid">
              <div class="f-group">
                <label class="f-label">Product Name *</label>
                <input class="f-input" id="pName" placeholder="e.g. Oversized Drop Tee">
              </div>
              <div class="f-group">
                <label class="f-label">Sale Price ₹ *</label>
                <input class="f-input" id="pPrice" placeholder="e.g. 1299" type="number">
              </div>
              <div class="f-group">
                <label class="f-label">Original Price ₹ (optional)</label>
                <input class="f-input" id="pOriginal" placeholder="e.g. 1799 (shows strikethrough)" type="number">
              </div>
              <div class="f-group">
                <label class="f-label">Category *</label>
                <select class="f-select" id="pCategory">
                  <option value="women">Women Wear</option>
                  <option value="men">Men Wear</option>
                  <option value="accessories">Accessories</option>
                  <option value="upper">Upper Wear</option>
                  <option value="bottom">Bottom Wear</option>
                </select>
              </div>
              <div class="f-group">
                <label class="f-label">Sizes * (comma separated)</label>
                <input class="f-input" id="pSizes" placeholder="XS, S, M, L, XL, XXL">
              </div>
              <div class="f-group">
                <label class="f-label">Badge</label>
                <select class="f-select" id="pBadge">
                  <option value="">None</option>
                  <option value="NEW">New</option>
                  <option value="HOT">Hot</option>
                  <option value="SALE">Sale</option>
                  <option value="LIMITED">Limited</option>
                </select>
              </div>
              <div class="f-group adm-form-full">
                <label class="f-label">Description</label>
                <textarea class="f-input" id="pDesc" placeholder="Describe this product..."></textarea>
              </div>
              <div class="f-group adm-form-full">
                <label class="f-label">Product Image URL</label>
                <input class="f-input" id="pImgUrl" placeholder="https://... (paste any image URL)">
                <div id="imgPreviewWrap" style="margin-top:8px;display:none;">
                  <img id="imgPreview" style="max-height:100px;max-width:180px;object-fit:cover;border:1px solid var(--border);" src="" alt="">
                </div>
              </div>
            </div>
            <div>
              <button class="adm-save" onclick="saveProduct()">SAVE PRODUCT</button>
              <button class="adm-cancel" onclick="hideProductForm()">CANCEL</button>
            </div>
          </div>

          <!-- Products Table -->
          <div class="prods-table-wrap">
            <div class="tbl-head">
              <span class="tbl-title">All Products (<span id="productCount">0</span>)</span>
              <button class="add-prod-btn" onclick="showProductForm()">+ ADD PRODUCT</button>
            </div>
            <div class="t-scroll">
              <table>
                <thead>
                  <tr>
                    <th>IMG</th>
                    <th>NAME</th>
                    <th>CATEGORY</th>
                    <th>SALE PRICE</th>
                    <th>ORIG PRICE</th>
                    <th>BADGE</th>
                    <th>ACTIONS</th>
                  </tr>
                </thead>
                <tbody id="productsTableBody"></tbody>
              </table>
            </div>
          </div>
        </div>

        <!-- Orders -->
        <div class="adm-section" id="admin-orders">
          <div class="adm-title">ORDERS</div>
          <div class="adm-sub">All COD orders — track and update status</div>
          <div id="ordersListAdmin"></div>
        </div>

        <!-- Settings -->
        <div class="adm-section" id="admin-settings">
          <div class="adm-title">SETTINGS</div>
          <div class="adm-sub">Configure your store</div>
          <div class="settings-block">
            <div class="settings-h">STORE INFO</div>
            <div class="f-group">
              <label class="f-label">Store Name (Display)</label>
              <input class="f-input" id="settingsStoreName" placeholder="SANJHEX">
            </div>
            <button class="adm-save" onclick="saveSettings()">SAVE SETTINGS</button>
          </div>
          <div class="settings-block">
            <div class="settings-h">ADMIN CREDENTIALS</div>
            <div class="f-group">
              <label class="f-label">Username</label>
              <input class="f-input" id="settingsUser" type="text">
            </div>
            <div class="f-group">
              <label class="f-label">New Password</label>
              <input class="f-input" id="settingsPass" type="password" placeholder="Leave blank to keep current">
            </div>
            <button class="adm-save" onclick="saveCredentials()">UPDATE CREDENTIALS</button>
          </div>
        </div>

      </div><!-- /admin-main -->
    </div><!-- /admin-layout -->
  </div>
</div><!-- /adminPage -->

<!-- ══════════════════════════════════════
  JAVASCRIPT
══════════════════════════════════════ -->
<script>
// ══ STATE ══
let cart = JSON.parse(localStorage.getItem('snjx_cart') || '[]');
let products = JSON.parse(localStorage.getItem('snjx_products') || 'null');
let orders = JSON.parse(localStorage.getItem('snjx_orders') || '[]');
let settings = JSON.parse(localStorage.getItem('snjx_settings') || 'null');
let adminCreds = JSON.parse(localStorage.getItem('snjx_creds') || 'null');
let currentPid = null, selSize = '', currentCat = 'all';

// Init defaults
if(!settings){settings={storeName:'SANJHEX'};localStorage.setItem('snjx_settings',JSON.stringify(settings));}
if(!adminCreds){adminCreds={user:'admin',pass:'sanjhex2025'};localStorage.setItem('snjx_creds',JSON.stringify(adminCreds));}
if(!products){
  products=[
    {id:'p1',name:'Void Drop Oversized Tee',price:1299,original:1799,category:'women',sizes:['XS','S','M','L','XL','XXL'],badge:'NEW',desc:'Ultra-oversized unisex tee with pigment wash. 100% heavyweight cotton. Dropped shoulders, raw hem detail.',img:'https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?w=600&q=80'},
    {id:'p2',name:'Shadow Cargo Pants',price:2499,original:3200,category:'women',sizes:['XS','S','M','L','XL'],badge:'HOT',desc:'Wide-leg cargo with multi-pocket utility design. Unisex fit. Ripstop cotton blend.',img:'https://images.unsplash.com/photo-1605518216938-7c31b7b14ad0?w=600&q=80'},
    {id:'p3',name:'Hex Block Hoodie',price:1899,original:null,category:'men',sizes:['S','M','L','XL','XXL'],badge:'',desc:'Double-layered hood, kangaroo pocket, ribbed cuffs. Heavyweight fleece. Unisex relaxed fit.',img:'https://images.unsplash.com/photo-1556821840-3a63f15732ce?w=600&q=80'},
    {id:'p4',name:'Distressed Utility Jacket',price:3499,original:null,category:'men',sizes:['S','M','L','XL'],badge:'LIMITED',desc:'Vintage wash jacket with contrast stitching. 4 utility pockets. Unisex street-ready silhouette.',img:'https://images.unsplash.com/photo-1591047139829-d91aecb6caea?w=600&q=80'},
    {id:'p5',name:'Worn Wash Shorts',price:999,original:1299,category:'bottom',sizes:['XS','S','M','L','XL','XXL'],badge:'SALE',desc:'Relaxed cut with elasticated waist. Stone-washed cotton. Unisex streetwear essential.',img:'https://images.unsplash.com/photo-1594938298603-c8148c4b4571?w=600&q=80'},
    {id:'p6',name:'Six-Panel Struct Cap',price:699,original:null,category:'accessories',sizes:['ONE SIZE'],badge:'',desc:'Structured 6-panel cap with tonal SANJHEX embroidery. Adjustable strap. Unisex.',img:'https://images.unsplash.com/photo-1588850561407-ed78c282e89b?w=600&q=80'},
    {id:'p7',name:'Drop Shoulder Sweatshirt',price:1599,original:2100,category:'upper',sizes:['S','M','L','XL','XXL'],badge:'NEW',desc:'Premium 380gsm fleece drop-shoulder sweatshirt. Ribbed cuffs and hem.',img:'https://images.unsplash.com/photo-1620799140408-edc6dcb6d633?w=600&q=80'},
    {id:'p8',name:'Ripstop Cargo Jogger',price:2199,original:null,category:'bottom',sizes:['XS','S','M','L','XL'],badge:'',desc:'Tapered cargo jogger in ripstop fabric. Elastic waist. Multiple pockets.',img:'https://images.unsplash.com/photo-1542272604-787c3835535d?w=600&q=80'},
  ];
  localStorage.setItem('snjx_products',JSON.stringify(products));
}

// ══ CURSOR ══
const cur=document.getElementById('cur'),curR=document.getElementById('curR');
let mx=0,my=0,rx=0,ry=0;
document.addEventListener('mousemove',e=>{
  mx=e.clientX;my=e.clientY;
  cur.style.cssText=`left:${mx-4}px;top:${my-4}px;`;
});
(function loop(){rx+=(mx-rx-16)*.14;ry+=(my-ry-16)*.14;curR.style.cssText=`left:${rx}px;top:${ry}px;`;requestAnimationFrame(loop);})();
document.querySelectorAll('button,a,[onclick]').forEach(el=>{
  el.addEventListener('mouseenter',()=>{cur.style.transform='scale(2)';curR.style.transform='scale(1.5)';});
  el.addEventListener('mouseleave',()=>{cur.style.transform='scale(1)';curR.style.transform='scale(1)';});
});

// ══ PAGES ══
function showPage(id){document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));document.getElementById(id).classList.add('active');window.scrollTo(0,0);}

// ══ TOAST ══
let _toastT;
function showToast(msg){const t=document.getElementById('toast');t.textContent=msg;t.classList.add('show');clearTimeout(_toastT);_toastT=setTimeout(()=>t.classList.remove('show'),2500);}

// ══ MOBILE NAV ══
function toggleMob(){document.getElementById('mobNav').classList.toggle('open');}

// ══ CATEGORIES ══
const catMeta={
  all:{eyebrow:'The Collection',title:'ALL DROPS',desc:'Every piece, every drop — all in one place.'},
  women:{eyebrow:'Curated for her',title:'WOMEN WEAR',desc:'From streetwear staples to statement pieces.'},
  men:{eyebrow:'Built for him',title:'MEN WEAR',desc:'Oversized, raw, and unapologetically bold.'},
  accessories:{eyebrow:'Complete the look',title:'ACCESSORIES',desc:'Caps, bags, and more to finish the fit.'},
  upper:{eyebrow:'On top',title:'UPPER WEAR',desc:'Hoodies, tees, jackets — tops that hit different.'},
  bottom:{eyebrow:'Below the waist',title:'BOTTOM WEAR',desc:'Cargos, shorts, and joggers with attitude.'},
  new:{eyebrow:'Just dropped',title:'NEW IN',desc:'The freshest pieces from the latest drop.'},
};

function showCat(cat){
  currentCat=cat;
  // update tabs
  document.querySelectorAll('.cat-tab').forEach(b=>b.classList.remove('active'));
  const tabMap={all:'tabAll',women:'tabWomen',men:'tabMen',accessories:'tabAcc',upper:'tabUpper',bottom:'tabBottom',new:'tabNew'};
  if(tabMap[cat])document.getElementById(tabMap[cat]).classList.add('active');
  // update nav active
  document.querySelectorAll('.nav-links a').forEach(a=>a.classList.remove('nav-active'));
  const navMap={all:'navAll',women:'navWomen',men:'navMen',accessories:'navAcc'};
  if(navMap[cat])document.getElementById(navMap[cat]).classList.add('nav-active');
  // update header
  const m=catMeta[cat]||catMeta.all;
  document.getElementById('secEyebrow').textContent=m.eyebrow;
  document.getElementById('secTitle').textContent=m.title;
  document.getElementById('secDesc').textContent=m.desc;
  renderProducts(cat);
}

function renderProducts(cat='all'){
  const grid=document.getElementById('productsGrid');
  const emp=document.getElementById('emptyState');
  let filtered=cat==='all'?products:cat==='new'?products.filter(p=>p.badge==='NEW'):products.filter(p=>p.category===cat);
  if(!filtered.length){grid.innerHTML='';emp.style.display='block';return;}
  emp.style.display='none';
  grid.innerHTML=filtered.map(p=>{
    const badgeClass={NEW:'new-badge',HOT:'hot-badge',SALE:'sale-badge',LIMITED:'ltd-badge'}[p.badge]||'';
    return `<div class="product-card" onclick="openPD('${p.id}')">
      <div class="p-img-wrap">
        ${p.badge?`<span class="p-badge ${badgeClass}">${p.badge}</span>`:''}
        ${p.img?`<img src="${p.img}" alt="${p.name}" loading="lazy" onerror="this.parentElement.innerHTML='<div class=p-img-placeholder>${p.name.slice(0,3).toUpperCase()}</div>'">`:
        `<div class="p-img-placeholder">${p.name.slice(0,3).toUpperCase()}</div>`}
        <div class="p-overlay">
          <button class="p-ov-btn" onclick="event.stopPropagation();quickAdd('${p.id}')">QUICK ADD</button>
          <button class="p-ov-btn ghost" onclick="event.stopPropagation();openPD('${p.id}')">VIEW DETAILS</button>
        </div>
      </div>
      <div class="p-info">
        <div class="p-cat-tag">${p.category}</div>
        <div class="p-name">${p.name}</div>
        <div class="p-meta">
          <div class="p-price">
            ${p.original?`<span class="orig">₹${p.original.toLocaleString('en-IN')}</span>`:''}
            ₹${p.price.toLocaleString('en-IN')}
          </div>
          <div class="p-sizes">${p.sizes.slice(0,4).join('·')}${p.sizes.length>4?'…':''}</div>
        </div>
      </div>
    </div>`;
  }).join('');
}

function scrollToCats(){document.getElementById('catsSection').scrollIntoView({behavior:'smooth'});}
function scrollToProducts(){document.getElementById('productsSection').scrollIntoView({behavior:'smooth'});}

// ══ PRODUCT DETAIL ══
let pdProd=null;
function openPD(id){
  pdProd=products.find(p=>p.id===id);if(!pdProd)return;
  currentPid=id;selSize='';
  document.getElementById('pdCat').textContent=pdProd.category.toUpperCase();
  document.getElementById('pdName').textContent=pdProd.name;
  document.getElementById('pdPrice').innerHTML=(pdProd.original?`<span class="orig">₹${pdProd.original.toLocaleString('en-IN')}</span>`:'')+`₹${pdProd.price.toLocaleString('en-IN')}`;
  document.getElementById('pdDesc').textContent=pdProd.desc||'';
  const img=document.getElementById('pdImg');
  if(pdProd.img){img.src=pdProd.img;img.style.display='block';}else img.style.display='none';
  document.getElementById('pdSizes').innerHTML=pdProd.sizes.map(s=>`<button class="sz-opt" onclick="selSz(this,'${s}')">${s}</button>`).join('');
  document.getElementById('productModal').classList.add('open');
  document.body.style.overflow='hidden';
}
function closePD(){document.getElementById('productModal').classList.remove('open');document.body.style.overflow='';}
function selSz(btn,s){document.querySelectorAll('.sz-opt').forEach(b=>b.classList.remove('sel'));btn.classList.add('sel');selSize=s;}

function pdAddToCart(){
  if(!selSize){showToast('Please select a size!');return;}
  addToCart(currentPid,selSize);closePD();openCart();
}
function pdBuyNow(){
  if(!selSize){showToast('Please select a size!');return;}
  addToCart(currentPid,selSize);closePD();openCheckout();
}
function quickAdd(id){
  const p=products.find(x=>x.id===id);if(!p)return;
  if(p.sizes.length===1){addToCart(id,p.sizes[0]);showToast('Added to bag!');return;}
  openPD(id);
}

// ══ CART ══
function addToCart(pid,size){
  const p=products.find(x=>x.id===pid);if(!p)return;
  const ex=cart.find(i=>i.pid===pid&&i.size===size);
  if(ex)ex.qty++;else cart.push({pid,size,qty:1,name:p.name,price:p.price,img:p.img||''});
  saveCart();updateCartUI();
}
function saveCart(){localStorage.setItem('snjx_cart',JSON.stringify(cart));}
function updateCartUI(){
  const total=cart.reduce((s,i)=>s+i.qty,0);
  document.getElementById('cartCount').textContent=total;
  const items=document.getElementById('cartItems');
  const foot=document.getElementById('cartFoot');
  const empty=document.getElementById('cartEmpty');
  if(!cart.length){empty.style.display='block';foot.style.display='none';return;}
  empty.style.display='none';foot.style.display='block';
  const subtotal=cart.reduce((s,i)=>s+i.price*i.qty,0);
  document.getElementById('cartTotal').textContent='₹'+subtotal.toLocaleString('en-IN');
  items.innerHTML='<div style="height:1px"></div>'+cart.map((i,idx)=>`
    <div class="cart-item">
      <div class="ci-img">${i.img?`<img src="${i.img}" alt="${i.name}" onerror="this.style.display='none'">`:''}
      </div>
      <div class="ci-det">
        <div class="ci-name">${i.name}</div>
        <div class="ci-size">SIZE: ${i.size}</div>
        <div class="ci-qty">
          <button class="qty-b" onclick="chgQty(${idx},-1)">−</button>
          <span class="qty-n">${i.qty}</span>
          <button class="qty-b" onclick="chgQty(${idx},1)">+</button>
        </div>
      </div>
      <div class="ci-price">₹${(i.price*i.qty).toLocaleString('en-IN')}</div>
      <button class="rm-item" onclick="rmItem(${idx})">✕</button>
    </div>
  `).join('');
}
function chgQty(idx,d){
  cart[idx].qty+=d;
  if(cart[idx].qty<=0)cart.splice(idx,1);
  saveCart();updateCartUI();
}
function rmItem(idx){cart.splice(idx,1);saveCart();updateCartUI();}
function clearCart(){cart=[];saveCart();updateCartUI();}
function openCart(){document.getElementById('cartOverlay').classList.add('open');document.getElementById('cartSidebar').classList.add('open');document.body.style.overflow='hidden';}
function closeCart(){document.getElementById('cartOverlay').classList.remove('open');document.getElementById('cartSidebar').classList.remove('open');document.body.style.overflow='';}

// ══ CHECKOUT ══
function openCheckout(){
  if(!cart.length){showToast('Your bag is empty!');return;}
  closeCart();
  const subtotal=cart.reduce((s,i)=>s+i.price*i.qty,0);
  document.getElementById('chkSummary').innerHTML=`
    <div class="order-sum-title">Order Summary</div>
    ${cart.map(i=>`<div class="o-row"><span>${i.name} (${i.size}) ×${i.qty}</span><span>₹${(i.price*i.qty).toLocaleString('en-IN')}</span></div>`).join('')}
    <div class="o-row total"><span>Total</span><span>₹${subtotal.toLocaleString('en-IN')}</span></div>
  `;
  document.getElementById('chkBody').style.display='block';
  document.getElementById('successWrap').style.display='none';
  document.getElementById('checkoutModal').classList.add('open');
  document.body.style.overflow='hidden';
}
function closeCheckout(){document.getElementById('checkoutModal').classList.remove('open');document.body.style.overflow='';}

function placeOrder(){
  const name=document.getElementById('cName').value.trim();
  const phone=document.getElementById('cPhone').value.trim();
  const address=document.getElementById('cAddress').value.trim();
  const city=document.getElementById('cCity').value.trim();
  const pin=document.getElementById('cPin').value.trim();
  const state=document.getElementById('cState').value;
  if(!name||!phone||!address||!city||!pin||!state){showToast('Please fill all required fields!');return;}
  if(phone.length<10){showToast('Enter a valid phone number!');return;}
  const oid='SJX'+Date.now().toString().slice(-8);
  const now=new Date();
  const order={
    id:oid,status:'Pending',
    date:now.toLocaleDateString('en-IN'),
    time:now.toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit'}),
    paymentMethod:'COD',
    customer:{name,phone,email:document.getElementById('cEmail').value,address,city,pin,state},
    items:cart.map(i=>({name:i.name,size:i.size,qty:i.qty,price:i.price})),
    total:cart.reduce((s,i)=>s+i.price*i.qty,0)
  };
  orders.push(order);
  localStorage.setItem('snjx_orders',JSON.stringify(orders));
  document.getElementById('chkBody').style.display='none';
  document.getElementById('successWrap').style.display='block';
  document.getElementById('successOid').textContent='ORDER ID: '+oid+' | COD — PAY ON DELIVERY';
  showToast('Order placed!');
}

// ══ ADMIN AUTH ══
function adminLogin(){
  const u=document.getElementById('loginUser').value.trim();
  const p=document.getElementById('loginPass').value;
  if(u===adminCreds.user&&p===adminCreds.pass){
    document.getElementById('loginErr').classList.remove('show');
    showPage('adminPage');renderAdmin();
  }else{document.getElementById('loginErr').classList.add('show');}
}
function adminLogout(){showPage('loginPage');document.getElementById('loginUser').value='';document.getElementById('loginPass').value='';}

// ══ ADMIN SWITCH ══
function switchAdmin(sec,el){
  document.querySelectorAll('.adm-menu-item').forEach(i=>i.classList.remove('active'));
  el.classList.add('active');
  document.querySelectorAll('.adm-section').forEach(s=>s.classList.remove('active'));
  document.getElementById('admin-'+sec).classList.add('active');
  if(sec==='products')renderAdminProducts();
  if(sec==='orders')renderAdminOrders();
  if(sec==='settings')loadSettings();
}

function renderAdmin(){
  orders=JSON.parse(localStorage.getItem('snjx_orders')||'[]');
  products=JSON.parse(localStorage.getItem('snjx_products')||'[]');
  const rev=orders.reduce((s,o)=>s+o.total,0);
  const pend=orders.filter(o=>o.status==='Pending').length;
  document.getElementById('statProducts').textContent=products.length;
  document.getElementById('statOrders').textContent=orders.length;
  document.getElementById('statRevenue').textContent='₹'+rev.toLocaleString('en-IN');
  document.getElementById('statPending').textContent=pend;
  if(pend>0)document.getElementById('orderDot').style.display='inline-block';
  else document.getElementById('orderDot').style.display='none';
  const dash=document.getElementById('dashboardOrders');
  if(!orders.length){dash.innerHTML='<div style="color:var(--muted);font-size:.75rem;padding:10px 0;font-family:Space Mono,monospace;">No orders yet.</div>';return;}
  dash.innerHTML=orders.slice().reverse().slice(0,6).map(o=>`
    <div style="display:flex;justify-content:space-between;align-items:center;padding:10px 0;border-bottom:1px solid var(--border);flex-wrap:wrap;gap:8px;">
      <div>
        <div style="font-family:'Space Mono',monospace;font-size:.68rem;color:var(--accent);">${o.id}</div>
        <div style="font-size:.82rem;">${o.customer.name} · ${o.customer.phone}</div>
        <div style="font-size:.68rem;color:var(--muted);">${o.date} ${o.time}</div>
      </div>
      <div style="display:flex;align-items:center;gap:12px;">
        <div style="font-family:'Barlow Condensed',sans-serif;font-weight:800;font-size:1.05rem;color:var(--accent);">₹${o.total.toLocaleString('en-IN')}</div>
        <span style="font-family:'Space Mono',monospace;font-size:.6rem;color:var(--muted);background:var(--surface2);padding:3px 8px;border:1px solid var(--border);">${o.status}</span>
        <span style="font-family:'Space Mono',monospace;font-size:.6rem;color:var(--accent);background:rgba(232,255,71,.1);padding:3px 8px;">COD</span>
      </div>
    </div>
  `).join('');
}

// ══ PRODUCTS ADMIN ══
function renderAdminProducts(){
  products=JSON.parse(localStorage.getItem('snjx_products')||'[]');
  document.getElementById('productCount').textContent=products.length;
  const tb=document.getElementById('productsTableBody');
  if(!products.length){tb.innerHTML='<tr><td colspan="7" style="text-align:center;padding:32px;color:var(--muted);font-family:Space Mono,monospace;font-size:.72rem;letter-spacing:2px;">NO PRODUCTS YET — ADD YOUR FIRST!</td></tr>';return;}
  tb.innerHTML=products.map(p=>`<tr>
    <td><div class="td-img">${p.img?`<img src="${p.img}" alt="" onerror="this.style.display='none'">`:''}
    </div></td>
    <td style="max-width:180px;">${p.name}</td>
    <td style="text-transform:capitalize;">${p.category}</td>
    <td style="font-family:'Space Mono',monospace;color:var(--accent);">₹${p.price.toLocaleString('en-IN')}</td>
    <td style="font-family:'Space Mono',monospace;color:var(--muted);">${p.original?'₹'+p.original.toLocaleString('en-IN'):'—'}</td>
    <td>${p.badge?`<span class="td-bdg on">${p.badge}</span>`:'<span class="td-bdg off">—</span>'}</td>
    <td>
      <button class="t-action" onclick="editProduct('${p.id}')">Edit</button>
      <button class="t-action del" onclick="delProduct('${p.id}')">Delete</button>
    </td>
  </tr>`).join('');
}
function showProductForm(reset=true){
  if(reset){
    document.getElementById('formTitle').textContent='ADD NEW PRODUCT';
    document.getElementById('editProductId').value='';
    ['pName','pPrice','pOriginal','pSizes','pBadge','pDesc','pImgUrl'].forEach(id=>document.getElementById(id).value='');
    document.getElementById('pCategory').value='women';
    document.getElementById('pSizes').value='XS, S, M, L, XL, XXL';
    hideImgPreview();
  }
  document.getElementById('productForm').style.display='block';
  document.getElementById('productForm').scrollIntoView({behavior:'smooth'});
}
function hideProductForm(){document.getElementById('productForm').style.display='none';hideImgPreview();}
function hideImgPreview(){document.getElementById('imgPreviewWrap').style.display='none';}
document.getElementById('pImgUrl').addEventListener('input',function(){
  if(this.value.trim()){
    document.getElementById('imgPreview').src=this.value.trim();
    document.getElementById('imgPreviewWrap').style.display='block';
  }else hideImgPreview();
});
function editProduct(id){
  const p=products.find(x=>x.id===id);if(!p)return;
  document.getElementById('formTitle').textContent='EDIT PRODUCT';
  document.getElementById('editProductId').value=p.id;
  document.getElementById('pName').value=p.name;
  document.getElementById('pPrice').value=p.price;
  document.getElementById('pOriginal').value=p.original||'';
  document.getElementById('pCategory').value=p.category;
  document.getElementById('pSizes').value=p.sizes.join(', ');
  document.getElementById('pBadge').value=p.badge||'';
  document.getElementById('pDesc').value=p.desc||'';
  document.getElementById('pImgUrl').value=p.img||'';
  if(p.img){document.getElementById('imgPreview').src=p.img;document.getElementById('imgPreviewWrap').style.display='block';}
  showProductForm(false);
}
function saveProduct(){
  const name=document.getElementById('pName').value.trim();
  const price=parseInt(document.getElementById('pPrice').value);
  const original=parseInt(document.getElementById('pOriginal').value)||null;
  const category=document.getElementById('pCategory').value;
  const sizes=document.getElementById('pSizes').value.split(',').map(s=>s.trim()).filter(Boolean);
  const badge=document.getElementById('pBadge').value;
  const desc=document.getElementById('pDesc').value.trim();
  const img=document.getElementById('pImgUrl').value.trim();
  const eid=document.getElementById('editProductId').value;
  if(!name||!price||!sizes.length){showToast('Fill Name, Price and Sizes!');return;}
  if(eid){const idx=products.findIndex(p=>p.id===eid);if(idx>-1)products[idx]={id:eid,name,price,original,category,sizes,badge,desc,img};}
  else products.push({id:'p'+Date.now(),name,price,original,category,sizes,badge,desc,img});
  localStorage.setItem('snjx_products',JSON.stringify(products));
  renderAdminProducts();renderProducts(currentCat);hideProductForm();
  showToast(eid?'Product updated!':'Product added!');
}
function delProduct(id){
  if(!confirm('Delete this product?'))return;
  products=products.filter(p=>p.id!==id);
  localStorage.setItem('snjx_products',JSON.stringify(products));
  renderAdminProducts();renderProducts(currentCat);showToast('Deleted.');
}

// ══ ORDERS ADMIN ══
function renderAdminOrders(){
  orders=JSON.parse(localStorage.getItem('snjx_orders')||'[]');
  const el=document.getElementById('ordersListAdmin');
  if(!orders.length){el.innerHTML='<div style="color:var(--muted);font-family:Space Mono,monospace;font-size:.75rem;letter-spacing:2px;padding:32px 0;">NO ORDERS YET</div>';return;}
  el.innerHTML=orders.slice().reverse().map(o=>`
    <div class="o-row-item">
      <div class="o-info">
        <div class="o-num">${o.id} <span style="background:rgba(232,255,71,.1);color:var(--accent);font-size:.55rem;padding:2px 7px;margin-left:6px;">COD</span></div>
        <div class="o-customer">${o.customer.name} · ${o.customer.phone}</div>
        <div class="o-date">${o.date} ${o.time} · ${o.customer.address}, ${o.customer.city}, ${o.customer.state} — ${o.customer.pin}</div>
        <div style="margin-top:5px;font-size:.72rem;color:var(--muted);">${o.items.map(i=>`${i.name} (${i.size}) ×${i.qty}`).join(' | ')}</div>
      </div>
      <div style="display:flex;flex-direction:column;align-items:flex-end;gap:8px;">
        <div class="o-amount">₹${o.total.toLocaleString('en-IN')}</div>
        <select class="o-status-sel" onchange="updateStatus('${o.id}',this.value)">
          <option ${o.status==='Pending'?'selected':''}>Pending</option>
          <option ${o.status==='Confirmed'?'selected':''}>Confirmed</option>
          <option ${o.status==='Shipped'?'selected':''}>Shipped</option>
          <option ${o.status==='Delivered'?'selected':''}>Delivered</option>
          <option ${o.status==='Cancelled'?'selected':''}>Cancelled</option>
        </select>
      </div>
    </div>
  `).join('');
}
function updateStatus(id,status){
  const o=orders.find(x=>x.id===id);
  if(o){o.status=status;localStorage.setItem('snjx_orders',JSON.stringify(orders));showToast('Status updated!');}
}

// ══ SETTINGS ══
function loadSettings(){
  settings=JSON.parse(localStorage.getItem('snjx_settings')||'{}');
  adminCreds=JSON.parse(localStorage.getItem('snjx_creds')||'{}');
  document.getElementById('settingsStoreName').value=settings.storeName||'SANJHEX';
  document.getElementById('settingsUser').value=adminCreds.user||'admin';
  document.getElementById('settingsPass').value='';
}
function saveSettings(){
  settings.storeName=document.getElementById('settingsStoreName').value.trim()||'SANJHEX';
  localStorage.setItem('snjx_settings',JSON.stringify(settings));
  showToast('Settings saved!');
}
function saveCredentials(){
  const u=document.getElementById('settingsUser').value.trim();
  const p=document.getElementById('settingsPass').value;
  if(!u){showToast('Username cannot be empty!');return;}
  adminCreds.user=u;if(p)adminCreds.pass=p;
  localStorage.setItem('snjx_creds',JSON.stringify(adminCreds));
  showToast('Credentials updated!');
}

// ══ INIT ══
renderProducts('all');
updateCartUI();
</script>
</body>
</html>
